# 5 PM Submission plan (6-hour crunch)

You have 10:47 AM → 5:00 PM = 6h13m. Original runbook called for 12-15h. Here's the cut-down path.

## Strategy

- **Use Colab's free T4** for training, not HF credits. T4 is enough for Qwen 2.5 0.5B + 150 episodes in ~2h.
- **Reserve $60 HF credits** as buffer for HF Space ops (re-deploys cost <$1 each in storage; the Space itself runs free on cpu-basic).
- **Trade model size for time**: 0.5B instead of 3B/7B. Smaller learning gain but a real upward curve is enough for "showing improvement in rewards" (20% of score).

## Step-by-step

### 10:50 AM — Open Colab + auth (5 min)

1. Open https://colab.research.google.com
2. File → Open notebook → GitHub → paste `https://github.com/parthtaneja0001/nyaya-mitra` → pick `training/train_grpo_colab.ipynb`
3. Runtime → Change runtime type → **T4 GPU** (Pro: try A100 if available). Save.
4. Cell 7 (auth): paste your **NEW** HF_TOKEN (after rotating the old one):
   ```python
   import os
   os.environ["HF_TOKEN"] = "hf_xxxxx"   # NEW, post-rotation token
   # optional: os.environ["WANDB_API_KEY"] = "..."
   ```
5. **Skip cell 11 (smoke run)** — local FakeChat smoke covered the orchestration. Time-saver.
6. **Replace cell 15 contents** with:
   ```
   !bash scripts/run_timeboxed.sh
   ```
   This is the timeboxed config (Qwen 0.5B + 150 episodes). Replaces the ~6h Phase 1.
7. Run cells 1-7 sequentially, then run **only** the modified cell 15 (skip 11).

### 11:00 AM-1:00 PM — Training runs unattended (~2h)

Watch the metrics jsonl in another tab if you want:
```
!tail -f /content/nyaya-mitra/training/dumps/phase1_timeboxed_metrics.jsonl
```

Training is pass/fail on whether the curve trends upward at all. If `rolling_mean_50` at step 150 > step 50, ship it.

### 1:00 PM-1:30 PM — Eval + plots regen + push artifacts (30 min)

The `run_timeboxed.sh` script auto-runs `scripts/render_demo_plots.py` at the end, which regenerates `demo/plots/*.png` with real data. To commit:

```
!cd /content/nyaya-mitra && git add training/dumps/ training/checkpoints/ demo/plots/ && git -c user.email='you@example.com' -c user.name='you' commit -m 'phase1 timeboxed artifacts' && git push
```

Or use cell 25 of the Colab (already wired for git push).

### 1:30 PM-1:45 PM — Re-deploy HF Space with new plots (15 min)

From your **Mac** (not Colab):
```
cd ~/meta-hackathon
git pull origin main                        # pull the artifacts the Colab pushed
export HF_TOKEN=hf_xxxxx                    # NEW token
./scripts/deploy_space.sh
```

Wait ~3 min for the Space to rebuild.

### 1:45 PM-2:30 PM — Update README/blog with real numbers (45 min)

In `eval/report_phase1_timeboxed.md` (auto-generated by the eval rerun) you'll have headline numbers: mean reward, integration solve rate, sensitivity F1.

Edit the `[fill in real numbers...]` placeholders in:
- `README.md`
- `demo/blog_post.md`

Push final commit.

### 2:30 PM-3:30 PM — Record 90-sec video (1h)

Use `demo/video_script.md` as the storyboard. Beats:
- 0:00-0:08 problem hook
- 0:08-0:18 LLM failure modes
- 0:18-0:35 4 gates + schema invariant
- 0:35-0:52 trained agent transcript
- 0:52-1:10 the numbers (real plots from timeboxed Phase 1)
- 1:10-1:25 architecture
- 1:25-1:30 URLs

Tools: any screen recorder + microphone. Upload to YouTube as **unlisted**.

### 3:30 PM-4:30 PM — Buffer (1h)

For: re-takes on the video, last-minute typos, additional Phase 1 episodes if time permits.

### 4:30 PM-5:00 PM — Submission (30 min)

Submit to the hackathon portal:
- Repo: https://github.com/parthtaneja0001/nyaya-mitra
- HF Space: https://huggingface.co/spaces/parthtaneja0001/nyaya-mitra-env
- Video: YouTube unlisted URL
- Blog: HF Hub or GitHub Pages
- README: top of the repo

## Fallbacks

**If training crashes or curve is flat (no improvement):**
- The local FakeChat plots already in `demo/plots/` are from a 30-ep smoke. They'll show flat lines but the components stack is real.
- Use those for the demo and document honestly: "Phase 1 timeboxed run shows the orchestration working; full Phase 1 future work."
- Score 5-10/20 on "showing improvement" instead of 15-20/20. Compensated by 35+/40 on environment innovation and 25+/30 on storytelling.

**If Colab kicks you off:**
- Pro: same T4, just reconnect and re-run from the latest checkpoint
- Free: longer 12h session; if disconnected, restart with the latest `adapter_step_*.lora` snapshot

**If second account needed:**
- Same `HF_TOKEN` slot in cell 7 — just paste a different account's token. Each token is independent.

## What's already submission-ready

- HF Space LIVE at https://huggingface.co/spaces/parthtaneja0001/nyaya-mitra-env (verified `/healthz` + reset+step round-trip)
- 499 tests pass on main, ruff clean
- 12 schemes + 8 frameworks + 14 seed profiles + 30 eval cases
- Demo blog post + video script written
- 6 demo plots rendered (from FakeChat smoke; will be overwritten by real Phase 1)

The remaining work is the actual GPU training run + recording the video. Both fit in 6 hours.
